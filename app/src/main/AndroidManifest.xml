<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saypresent">

    <application
        android:allowBackup="true"
        android:icon="@drawable/say_present_logo_single"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Event"></activity>
        <activity android:name=".addEvent" /> <!-- FOR TESTING -->
        <activity android:name=".checkpoint_list" />
        <activity android:name=".EventListActivity" />
        <activity android:name=".TestingActivity" /> <!-- FOR TESTING -->
        <activity android:name=".Dashboard" />
        <activity android:name=".SignUp" />
        <activity android:name=".LoginPage" />
        <activity android:name=".Login" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>